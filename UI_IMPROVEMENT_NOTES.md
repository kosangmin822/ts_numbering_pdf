# UI 개선 사항 (shadcn 스타일 적용)

## 개요
TS Numbering for PDF 애플리케이션의 UI를 shadcn 디자인 시스템을 기반으로 개선하였습니다.

## 주요 변경 사항

### 1. 전역 스타일시트 시스템 구축
- **파일**: `ui/styles.py`
- shadcn 스타일의 색상 팔레트 적용
- 일관된 타이포그래피와 간격 정의
- 모든 Qt 위젯에 대한 통일된 스타일링

#### 색상 팔레트
- **Primary**: `#18181B` (진한 회색)
- **Muted**: `#F4F4F5` (밝은 회색)
- **Border**: `#E4E4E7` (경계선)
- **Destructive**: `#EF4444` (빨강)

### 2. 다이얼로그 개선
모든 다이얼로그를 shadcn 스타일로 재디자인:

#### NewProjectDialog
- 깔끔한 레이아웃과 간격
- 명확한 시각적 계층 구조
- 개선된 버튼 스타일링

#### InsertDialog
- 라디오 버튼 옵션 개선
- 일관된 버튼 배치
- 향상된 가독성

#### StampSettingsDialog
- 그룹박스 스타일 개선
- 슬라이더와 입력 필드 정렬 개선
- 더 나은 시각적 피드백

#### AppendPdfDialog
- 단순화된 버튼 레이아웃
- 명확한 옵션 제시

#### NumberingModeDialog
- 큰 버튼으로 선택하기 쉽게 개선
- 명확한 설명 텍스트

#### SaveOptionsDialog
- 라디오 버튼 옵션 개선
- 일관된 레이아웃

### 3. 메인 윈도우 스타일링
- 전역 스타일시트 적용
- 일관된 버튼, 입력 필드, 테이블 스타일
- 개선된 메뉴바 및 툴바 디자인

### 4. 위젯 스타일 통일

#### 버튼
- Primary 버튼: 어두운 배경
- Secondary 버튼: 밝은 배경
- Destructive 버튼: 빨간색 배경
- 일관된 패딩과 경계선 반경

#### 입력 필드
- 부드러운 경계선
- 포커스 시 강조 표시
- 일관된 높이와 간격

#### 테이블
- 깔끔한 헤더 스타일
- 교대 행 색상
- 선택 시 명확한 강조

#### 스크롤바
- 최소주의 디자인
- 호버 시 색상 변경
- 둥근 모서리

### 5. 썸네일 뷰
- 둥근 모서리 적용
- 개선된 활성/비활성 상태 시각화
- shadcn 색상 팔레트 사용

## 디자인 철학

### shadcn 스타일 원칙
1. **최소주의**: 불필요한 장식 제거
2. **일관성**: 모든 컴포넌트에 동일한 디자인 언어 적용
3. **접근성**: 명확한 대비와 시각적 계층 구조
4. **현대성**: 모던하고 깔끔한 외관

### 색상 사용
- 중성 회색 계열을 주로 사용
- 액센트 색상은 중요한 액션에만 사용
- 오류/경고는 명확한 색상으로 표시

### 타이포그래피
- 시스템 폰트 스택 사용
- 명확한 글꼴 크기 계층
- 적절한 글꼴 두께 사용

### 간격과 레이아웃
- 일관된 패딩: 8px, 12px, 16px, 24px
- 그리드 기반 레이아웃
- 적절한 여백으로 가독성 향상

## 기술 구현

### 스타일시트 적용 방법
```python
from ui.styles import GLOBAL_STYLESHEET

# 애플리케이션 또는 위젯에 적용
widget.setStyleSheet(GLOBAL_STYLESHEET)
```

### 버튼 스타일 변형
```python
# Secondary 버튼
button.setProperty("buttonStyle", "secondary")

# Destructive 버튼
button.setProperty("buttonStyle", "destructive")
```

## 향후 개선 사항

### 다크 모드 지원
- `DARK_COLORS` 팔레트가 이미 정의되어 있음
- 다크 모드 토글 기능 추가 예정

### 애니메이션
- 버튼 호버 효과
- 다이얼로그 전환 애니메이션
- 페이지 전환 효과

### 아이콘 통일성
- 아이콘 세트를 통일된 디자인으로 교체 고려
- SVG 아이콘 사용 검토

## 파일 변경 내역

### 신규 파일
- `ui/styles.py`: 전역 스타일시트 및 색상 정의

### 수정된 파일
- `ui/dialogs.py`: 모든 다이얼로그 재디자인
- `ui/views.py`: ThumbnailLabel 스타일 개선
- `ui/__init__.py`: 스타일 모듈 export 추가
- `main.py`: 전역 스타일시트 import 및 적용

## 테스트 가이드

### 시각적 확인 사항
1. 모든 다이얼로그가 일관된 스타일로 표시되는지 확인
2. 버튼 hover/press 상태가 명확히 표시되는지 확인
3. 입력 필드 포커스 상태가 명확히 표시되는지 확인
4. 테이블 선택이 명확히 표시되는지 확인
5. 스크롤바가 부드럽게 작동하는지 확인

### 기능적 확인 사항
1. 모든 다이얼로그가 정상적으로 열리고 닫히는지 확인
2. 버튼 클릭이 정상적으로 작동하는지 확인
3. 입력 필드에서 텍스트 입력이 정상적으로 작동하는지 확인
4. 테이블 선택 및 편집이 정상적으로 작동하는지 확인

## 문의 및 피드백
UI 개선에 대한 문의사항이나 피드백이 있으시면 프로젝트 관리자에게 연락해주세요.

